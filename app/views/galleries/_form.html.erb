<%= form_with(model: gallery, local: true) do |form| %>
  <% if gallery.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(gallery.errors.count, "error") %> prohibited this gallery from being saved:</h2>

      <ul>
      <% gallery.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group row">
    <%= form.label 'Titlu', :class => 'col-sm-2 col-form-label'%>
    <div class="col-sm-10" style="background-color: white">
        <%= form.text_field :title, :class => 'form-control-plaintext' %>
    </div>
  </div>

  <div class="form-group row">
    <%= form.label 'Descriere', :class => 'col-sm-2 col-form-label'%>
    <div class="col-sm-10" style="background-color: white">
        <%= form.text_field :description, :class => 'form-control-plaintext'%>
    </div>
  </div>

  <div class="form-group row">
    <%= form.label 'Miniatură', :class => 'col-sm-2 col-form-label'%>
    <%= form.file_field :thumbnail, :class => 'form-control-image' %>
  </div>

  <div class="form-group row">
    <%= form.submit 'Salvează albumul', class: 'btn btn-danger my-2 my-sm-0', role: 'button'%>
  </div>
<% end %>
