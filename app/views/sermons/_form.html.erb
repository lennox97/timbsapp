
<%= form_with(model: sermon, local: true) do |form| %>
  <% if sermon.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sermon.errors.count, "error") %> prohibited this sermon from being saved:</h2>

      <ul>
      <% sermon.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group row">
    <%= form.label 'Titlu', :class => 'col-sm-2 col-form-label'%>
    <div class="col-sm-10" style="background-color: white">
      <%= form.text_field :title, :class=>'form-control-plaintext'%>
    </div>
  </div>

  <div class="form-group row">
    <%= form.label 'Predicator', :class => 'col-sm-2 col-form-label'%>
    <div class="col-sm-10" style="background-color: white">
      <%= form.text_field :preacher, :class => 'form-control-plaintext' %>
    </div>
  </div>

  <div class="form-group row">
    <%= form.label 'Loc', :class => 'col-sm-2 col-form-label' %>
    <div class="col-sm-10" style="background-color: white">
      <%= form.text_field :place, :class => 'form-control-plaintext' %>
    </div>
  </div>

  <div class="form-group row">
    <%= form.label 'Data', :class => 'col-sm-2 col-form-label' %>
    <div class="col-sm-10">
      <%= form.date_select :date, :class=> 'form-control'%>
    </div>
  </div>

  <div class="form-group row">

      <%= form.label :audio, :class => 'col-sm-2 col-form-label' %>
      <div class="col-sm-10">
        <%= form.file_field :audio %>
      </div>

  </div>

  <div class="form-group row">
    <%= form.label :video, :class => 'col-sm-2 col-form-label' %>
    <div class="col-sm-10">
      <%= form.file_field :video %>
    </div>
  </div>

  <div class="form-group row">
    <%= form.submit 'Salvează predica', class: 'btn btn-danger my-2 my-sm-0', role: 'button'%>
  </div>
<% end %>
